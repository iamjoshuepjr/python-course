# create a program to calculate the area of a rectangle given its height and weight

height = float(input("Enter the height here: "))
weight = float(input("Enter the weight here: "))

area = height * weight

print(f"The area by values {height} and {weight} is: {area}")